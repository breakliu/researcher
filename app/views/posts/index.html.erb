<h1><%= t('.title') %></h1>

<table>
  <tr>
    <th><%= t('.table_title') %></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= link_to t('.show'), post %></td>
    <td><%= link_to t('.edit'), edit_post_path(post) %></td>
    <td><%= link_to t('.destroy'), post, confirm: t('.are_you_sure'), method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t('.new_post'), new_post_path %>
